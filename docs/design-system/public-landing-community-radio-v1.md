# Landing design-system decision

The existing Basira semantic tokens, buttons, cards, focus treatment, typography, color
contrast, light/dark themes, and motion preferences cover the approved landing UX. The
feature may compose these tokens in feature-local styles; no new shared primitive or token is
required. Radio controls must use native button/range semantics and existing focus tokens.
