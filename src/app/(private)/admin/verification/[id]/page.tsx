import { ReviewDecision } from "@/features";
export default function Page() { return <ReviewDecision reviewCase={{ id: "verification-1", status: "قيد المراجعة", priority: "عادية", kind: "verification", ageLabel: "يومان", version: 1 }} />; }
